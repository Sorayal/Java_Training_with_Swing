package practice;

import javax.swing.*;

public class MainFrame extends JFrame {
    public MainFrame(){
        super("Practice");

        setSize(800, 600);
        setVisible(true);
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
    }
}
